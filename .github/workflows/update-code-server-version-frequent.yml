name: Update Code-Server Version (Frequent)

on:
  schedule:
    # Run every 6 hours
    - cron: '0 */6 * * *'
  workflow_dispatch: # Allow manual triggering

jobs:
  update-version:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Get latest Code-Server version
        id: get-version
        run: |
          # Get the latest version from Docker Hub
          LATEST_VERSION=$(curl -s "https://registry.hub.docker.com/v2/repositories/linuxserver/code-server/tags/" | \
            jq -r '.results[] | select(.name | test("^[0-9]+\\.[0-9]+\\.[0-9]+$")) | .name' | \
            sort -V | tail -n 1)
          
          # Extract major.minor.patch version
          VERSION=$(echo $LATEST_VERSION | grep -oE '^[0-9]+\.[0-9]+\.[0-9]+')
          
          if [ -z "$VERSION" ]; then
            echo "Error: Could not extract version from $LATEST_VERSION"
            exit 1
          fi
          
          echo "Latest Code-Server version: $VERSION"
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Check if version needs updating
        id: check-version
        run: |
          CURRENT_VERSION=$(grep -A 1 "Linuxserver CodeServer" .bunnyshell/templates/code-server/template.yaml | grep "version:" | sed 's/.*version: //' | tr -d "'")
          LATEST_VERSION="${{ steps.get-version.outputs.version }}"
          
          echo "Current version: $CURRENT_VERSION"
          echo "Latest version: $LATEST_VERSION"
          
          if [ "$CURRENT_VERSION" = "$LATEST_VERSION" ]; then
            echo "No update needed"
            echo "update_needed=false" >> $GITHUB_OUTPUT
          else
            echo "Update needed"
            echo "update_needed=true" >> $GITHUB_OUTPUT
          fi

      - name: Update template.yaml
        if: steps.check-version.outputs.update_needed == 'true'
        run: |
          # Update the version in template.yaml
          # Update the version line, preserving indentation
          sed -i -E "s/^([[:space:]]*)version: '[0-9]+\.[0-9]+\.[0-9]+'/\1version: '${{ steps.get-version.outputs.version }}'/" .bunnyshell/templates/code-server/template.yaml
          
          # Verify the change
          echo "Updated template.yaml:"
          grep -A 1 "Linuxserver CodeServer" .bunnyshell/templates/code-server/template.yaml

      - name: Create Pull Request
        if: steps.check-version.outputs.update_needed == 'true'
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "chore: update Code-Server to version ${{ steps.get-version.outputs.version }}"
          title: "🔄 Update Code-Server to version ${{ steps.get-version.outputs.version }}"
          body: |
            ## 🔄 Code-Server Version Update (Frequent)
            
            This PR automatically updates the Code-Server version in the template.
            
            **Changes:**
            - Updated Code-Server to latest version **${{ steps.get-version.outputs.version }}**
            
            **Why this update:**
            - Keeps the template up-to-date with the latest Code-Server features
            - Ensures security patches are included
            - Maintains compatibility with latest Docker images
            
            **Automated by:** GitHub Action `update-code-server-version-frequent.yml`
            
            ---
            *This PR was automatically generated. Please review and merge if everything looks good.*
          branch: update/code-server-version-frequent
          delete-branch: true
          labels: |
            automated
            dependencies
            code-server
            frequent-update
          assignees: ${{ github.repository_owner }}

      - name: Comment on PR if no update needed
        if: steps.check-version.outputs.update_needed == 'false' && github.event_name == 'workflow_dispatch'
        run: |
          echo "✅ No update needed. Current version is already up-to-date."
